---
title: "Pricing Card"
description: "Individual plan card for dashboards, billing pages, upgrade modals, and custom layouts"
---

import { PricingCardPreview } from '/snippets/pricing-card-preview.jsx';

Use **Pricing Card** to render a single plan anywhere in your product. Compose multiple cards yourself, or use [Pricing Table](/docs/exportable-ui/pricing-widget/pricing-table) for a ready-made grid.

<Tabs>
  <Tab title="Preview">
    <PricingCardPreview />
  </Tab>
  <Tab title="Code">
    ```tsx
    <PricingCard {...plan} useModernChrome onSelectPlan={handleSelect} />
    ```
  </Tab>
</Tabs>

## What PricingCard supports

- Feature list
- Custom badges
- Billing cadence
- Usage pricing
- Trial messaging
- CTA buttons

## Where to use PricingCard

- Dashboard
- Billing page
- Subscription selector
- Upgrade modal
- Checkout

## Importing PricingCard

```tsx
import '@flexprice/ui/style.css';
import { PricingCard } from '@flexprice/ui';
```

## Rendering PricingCard

```tsx
<PricingCard {...plan} useModernChrome onSelectPlan={handleSelect} />
```

Pass a presentational plan from the Flexprice SDK, REST, your backend, or static JSON. See [Ways to provide data](/docs/exportable-ui/overview#ways-to-provide-data-to-the-components).

## Mapping API data into PricingCard

```ts
import { adaptPlanToCard, filterAndSortPlans } from '@flexprice/ui';

const [plan] = filterAndSortPlans(plansWithData, 'USD', 'MONTHLY').map((p) =>
  adaptPlanToCard(p, grants)
);
```

## Related pricing widgets

<CardGroup cols={2}>
  <Card icon="/images/developers/icons/api-reference.svg" href="/docs/exportable-ui/overview" title="Overview">
    Install Flexprice UI, choose a data source, theme, and integrate components.
  </Card>
  <Card icon="/images/developers/icons/table.svg" href="/docs/exportable-ui/pricing-widget/pricing-table" title="Pricing Table">
    Comparison table for multiple plans, features, and entitlements.
  </Card>
</CardGroup>
